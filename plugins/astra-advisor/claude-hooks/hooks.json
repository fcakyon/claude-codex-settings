{
  "description": "Stages the real conversation for the astra-advisor reviewer instead of only the caller's prompt",
  "hooks": {
    "SubagentStart": [
      {
        "matcher": "^(astra-advisor:)?astra-advisor$",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/claude-hooks/scripts/forward_transcript.mjs"]
          }
        ]
      }
    ]
  }
}
